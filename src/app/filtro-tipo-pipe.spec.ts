import { FiltroTipoPipe } from './filtro-tipo-pipe';

describe('FiltroTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
